# Phase 7c (new): SSL-init fine-tune with UNFROZEN encoder, fold 0.
# Initializes only from the fold-0 SSL checkpoint (SSL excluded fold 0 during
# pretraining; plan 23.6). Per-fold config so fold-N runs never init from a
# checkpoint that saw fold N.
name: e7c_sslinit_unfrozen_fold3_v1
method: separate_raw_ot_hierarchical_sslinit_unfrozen_v1
fold_version: v1
tasks: [LEOP, PERG]
leop_cohort: primary_nine_step
outer_folds: [3]
seeds: [1001, 2002, 3003]
epochs: 200
batch_size: 8
lr: 0.0001
weight_decay: 0.0001
warmup_epochs: 5
patience: 25
grad_clip: 1.0
device: cuda
max_steps_per_epoch: null
output_subdir: separate_raw_ot_hierarchical_sslinit_unfrozen_v1
n_bootstrap_reps: 2000
bootstrap_seed: 424242
confidence: 0.95
init_ssl: artifacts/results/ssl_pretrain_v1/fold3/final.pt
freeze_encoders: false
routing_graph: correct
label_frac: 1.0
subset_seed: 9001
resume: false