# Plan §35 item 20 / Phase 8: pathway-constrained sharing with late-only
# shared expert (graph control "correct"). Wrong/random/full/none controls
# are the same file with routing_graph swapped; the config hash and run
# manifest distinguish them, and parameter counts are identical across
# controls (Module 21.14).
name: e8_pathway_graph_full_v1
method: pathway_graph_full_v1
fold_version: v1
tasks: [LEOP, PERG]
leop_cohort: primary_nine_step
outer_folds: [0, 1, 2, 3, 4]
seeds: [1001, 2002, 3003]
epochs: 200
batch_size: 8
lr: 0.0001
weight_decay: 0.0001
warmup_epochs: 5
patience: 25
grad_clip: 1.0
device: cuda
max_steps_per_epoch: null
output_subdir: pathway_graph_full_v1
n_bootstrap_reps: 2000
bootstrap_seed: 424242
confidence: 0.95
init_ssl: null
routing_graph: full
